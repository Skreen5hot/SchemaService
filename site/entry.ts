/**
 * Browser entry point for BIBSS demo site.
 *
 * Bundled by esbuild into an IIFE that exposes BIBSS on the global scope.
 */

import { createBIBSS } from "../src/kernel/index.js";
export { createBIBSS };
